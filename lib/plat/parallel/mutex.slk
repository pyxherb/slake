module plat.parallel.mutex;

public native class Mutex {
	public native fn new();

	public native fn lock();
	public native fn unlock();
	public native fn tryLock(): bool;
	public native fn isLocked(): bool;
}
