module plat.parallel.thread;

use std.core.effect;
use std.core.except;

public interface IWork {
	public operator()();
}

class Thread final {
	public native fn Thread(work: IWork);
	public native fn Thread(work: IWork, stackSize: usize);
}

public native fn yield() out ESchedule;
