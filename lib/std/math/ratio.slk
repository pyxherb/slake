pub trait IRatio<T> {
	@T -> numerator;
	@T -> denominator;
}

pub class Ratio<T>: IRatio<@T> {
	@T _num, _den;

	pub @T -> numerator {
		this { return _num; }
		= { _num = num; }
	}

	pub @T -> denominator {
		this { return _den; }
		= { _den = den; }
	}
}
