#ifndef _SLAKE_CONFIG_H_
#define _SLAKE_CONFIG_H_

#include <cstdint>

#if @SLAKE_WITH_STDLIB@==TRUE
#define SLAKE_WITH_STDLIB true
#else
#define SLAKE_WITH_STDLIB false
#endif

#define SLAKE_STACK_MAX @SLAKE_STACK_MAX@
#define SLAKE_NATIVE_STACK_MAX @SLAKE_NATIVE_STACK_MAX@

#if @SLAKE_ENABLE_DEBUGGER@==TRUE
#define SLAKE_ENABLE_DEBUGGER true
#else
#define SLAKE_ENABLE_DEBUGGER false
#endif

#endif
